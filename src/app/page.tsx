export default function Page({
  searchParams: _searchParams,
}: {
  searchParams: Record<string, string | string[] | undefined>
}) {
  return (
    <main className="flex min-h-screen flex-col items-center p-24">todo</main>
  )
}
